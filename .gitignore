/node_modules
/.next
/out
.DS_Store
*.log
.vercel
.env
.env*.local

# Python virtualenv for the ConnectLife bridge
.venv/

certificates
# Local development CA — belongs on your devices, not in a public repo.
public/rootCA.crt
certificates/

# Family photographs — never in a public repo, and not in git history either.
data/
